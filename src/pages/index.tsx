import { Center } from '@chakra-ui/react';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Center w="full" h="100vh">
      Center
    </Center>
  );
};

export default Home;

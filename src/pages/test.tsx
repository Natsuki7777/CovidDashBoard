import { Center } from '@chakra-ui/react';
import { NextPage } from 'next';

const Test: NextPage = () => {
  return (
    <Center w="full" h="full">
      test
    </Center>
  );
};

export default Test;

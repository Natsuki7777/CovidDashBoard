import { Center } from '@chakra-ui/react';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Center w="full" h="full">
      Home
    </Center>
  );
};

export default Home;
